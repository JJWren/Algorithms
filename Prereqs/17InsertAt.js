/**
 * Returns back array with value inserted at specified position: ([ 0, 1, 2, 3], "dog", 1) => [ 0, "dog", 1, 2, 3]
 * @param {Array} array
 * @param {} value
 * @param {Number} position
 */
function insertAt(array, value, position) {
    let subArr1 = [];
    return array;
}
